# Wallet Profiler Web Environment Variables

# For production: URL of the Python profiler API (Railway)
# If not set, falls back to local CLI execution
PROFILER_API_URL=https://your-railway-app.railway.app
